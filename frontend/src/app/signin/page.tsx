'use client';

/**
 * Signin page - wraps SigninForm which handles all styling via authForm.css
 */

import SigninForm from '../../components/auth/SigninForm';

export default function SigninPage() {
  return <SigninForm />;
}
