'use client';

/**
 * Signup page - wraps SignupForm which handles all styling via authForm.css
 */

import SignupForm from '../../components/auth/SignupForm';

export default function SignupPage() {
  return <SignupForm />;
}
